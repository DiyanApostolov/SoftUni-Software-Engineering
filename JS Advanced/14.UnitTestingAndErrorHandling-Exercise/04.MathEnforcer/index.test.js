const { assert } = require('chai');
const mathEnforcer = require('./index.js');

describe('mathEnforcer function tests', ()=>{
    it('Shoud return undefined with string', ()=>{
        assert.equal(mathEnforcer.addFive('TEST'), undefined);
    }); 
    it('', ()=>{
        assert.equal();
    });
});